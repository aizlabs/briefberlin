# Secrets Setup

The active Pages workflow deploys committed public output only and should not receive private/manual source article text.

Configure only the secrets needed for deployment or optional notifications. Do not store manual source articles in repository secrets, workflow variables, workflow artifacts, logs, or generated posts.
